# Shared contracts

Generated clients and cross-runtime fixtures belong here. The source of truth for public HTTP behavior is `docs/api/openapi.yaml`; do not manually duplicate API types between frontend and backend.
