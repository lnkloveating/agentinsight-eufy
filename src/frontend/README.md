# Frontend

React/TypeScript shell for the five core experiences. Feature folders exist now so UI development can start against the OpenAPI contract while backend behavior is built independently.

```powershell
npm install
npm run frontend:dev
```
